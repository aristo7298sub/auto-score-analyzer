# Local-All（纯本地，除 Azure OpenAI 外）

# -------------------- Database --------------------
# SQLite（本地文件）
DATABASE_URL=sqlite:////app/data/score_analyzer.db

# -------------------- Storage --------------------
# local | azure
STORAGE_TYPE=local

# -------------------- Azure OpenAI (required) --------------------
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_API_KEY=<your-key>
AZURE_OPENAI_API_VERSION=2025-01-01-preview
AZURE_OPENAI_DEPLOYMENT_NAME=<deployment>

# -------------------- Optional --------------------
DEBUG=false

# -------------------- Email (Stage-1 auth) --------------------
# dev | acs | disabled
EMAIL_PROVIDER=dev
EMAIL_LOG_CODES_IN_DEV=true

# ACS Email (required when EMAIL_PROVIDER=acs)
ACS_EMAIL_CONNECTION_STRING=<your-acs-connection-string>
ACS_EMAIL_SENDER=no-reply@<primary-domain>
